let mobile = 'node'

let esy = ` ${mobile}.js® is a JavaScript runtime built on Chrome's 
            V8 JavaScript engine. 
            Node.js uses an event-driven, non-blocking I/O model 
            scalabe = ${10 * 10}%
            non-blocking = ${10 * 10}%
          `
console.log(esy)