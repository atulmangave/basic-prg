import { Capacitor } from './cap';
import { GpsListener } from './gps';
export class Device {

    private cap: number

    constructor(
        cap = 56
    ) {
        this.cap = cap
    }
    mnf(cntry?: string) {

    }

    capCapcity(): string | number {
        return `codekul`
    }

    addCaps(obj: { [key: string]: any }) {

    }

    newCap(obj: Capacitor) {

    }

    otherResi(...rs: number[]) {

    }

    capGps( obj : GpsListener) {
        
    }
}

export const PI = 3.14